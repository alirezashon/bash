# ⚡ Quick Start Guide

راهنمای سریع برای شروع کار با سیستم مدیریت محتوا

## 🚀 شروع سریع (5 دقیقه)

### 1. نصب پیش‌نیازها

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y jq curl file bc

# macOS
brew install jq
```

### 2. تنظیم اولیه

```bash
cd automations

# دادن دسترسی اجرا
chmod +x *.sh

# کپی فایل کانفیگ
cp upload_config.env.example upload_config.env
```

### 3. اجرای دمو

```bash
# مشاهده دمو کامل
./demo.sh
```

### 4. استفاده واقعی

```bash
# اجرای سیستم آپلود
./upload.sh

# اجرای سیستم مدیریت
./content_manager.sh
```

## 📝 مثال سریع

### آپلود یک فایل

```bash
# 1. فایل را در پوشه to_upload قرار دهید
mkdir -p to_upload
cp your_video.mp4 to_upload/

# 2. اجرای اسکریپت
./upload.sh

# 3. از منو:
#    - گزینه 1: Select files
#    - انتخاب فایل
#    - انتخاب پلتفرم (مثلاً Telegram)
#    - وارد کردن عنوان
#    - آپلود خودکار!
```

### مشاهده آمار

```bash
./content_manager.sh

# گزینه 1: View Dashboard
# مشاهده تمام آمار و گزارش‌ها
```

### خروجی گزارش HTML

```bash
./content_manager.sh

# گزینه 5: Export Reports
# انتخاب HTML
# باز کردن فایل در مرورگر
```

## 🎯 کاربردهای رایج

### روزانه: آپلود محتوا
```bash
./upload.sh  # آپلود به پلتفرم‌های انتخابی
```

### هفتگی: بررسی آمار
```bash
./content_manager.sh  # گزینه 1: Dashboard
```

### ماهانه: خروجی گزارش
```bash
./content_manager.sh  # گزینه 5: Export Reports
```

## ⚙️ تنظیمات سریع

### تغییر پوشه منبع
```bash
export UPLOAD_SOURCE_DIR="./my_media"
./upload.sh
```

### تغییر تعداد آپلود همزمان
```bash
export UPLOAD_MAX_CONCURRENT=8
./upload.sh
```

## 🔗 لینک‌های مفید

- 📖 [مستندات کامل](./PROJECT_README.md)
- 🎬 [اجرای دمو](./demo.sh)
- ⚙️ [فایل کانفیگ](./upload_config.env.example)

## ❓ سوالات متداول

**Q: چطور API keys را تنظیم کنم؟**
A: فایل `upload_config.env` را ویرایش کنید و توکن‌های خود را وارد کنید.

**Q: دیتابیس کجا ذخیره می‌شود؟**
A: در پوشه `./content_db/content_database.json`

**Q: چطور پشتیبان بگیرم؟**
A: در منوی `content_manager.sh` گزینه 6 را انتخاب کنید.

**Q: آیا می‌توانم از API استفاده کنم؟**
A: بله! خروجی JSON را می‌توانید در سیستم‌های دیگر استفاده کنید.

---

**💡 نکته:** برای اطلاعات بیشتر، `PROJECT_README.md` را مطالعه کنید.







