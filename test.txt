4) شرط‌ها (if)
if [ "$age" -gt 18 ]; then
  echo "adult"
else
  echo "child"
fi


کلیدواژه‌های مهم:

if

[ condition ]

then

else

fi (پایان)

5) حلقه‌ها (for)
for file in *; do
  echo $file
done


کل دستور:

for ... in ...; do
    ...
done

6) دستور case
case $name in
  ali)
    echo "Hello Ali"
    ;;
  reza)
    echo "Hi Reza"
    ;;
  *)
    echo "Unknown"
    ;;
esac


برای چندین شرط مختلف.

7) اجرای برنامه‌ها
ls
cd folder
mkdir new
rm file.txt

8) اجرای اسکریپت

فایل را بساز:

nano script.sh


اولین خط همیشه:

#!/bin/bash


اجازه اجرا بده:

chmod +x script.sh


اجرا:

./script.sh

🎯 شِل‌اِپت (shopt) چیه؟

shopt = Shell Options

برای کنترل رفتار داخلی Bash
(چیزهایی که با set قابل انجام نیست)

مثال:

shopt -s dotglob


-s = set (فعال کردن)

dotglob = شامل کردن فایل‌های مخفی در الگوهای *

🎯 مهم‌ترین رفتارها در Bash
✔ گلوبینگ (glob)

یعنی استفاده از wildcard:

*.txt
foo*
*config*


Bash الگو را با فایل‌های واقعی تطبیق می‌دهد.

✔ wildcard مهم‌ها:
علامت	معنی
*	هر چیزی
?	یک کاراکتر
[abc]	یکی از حروف a,b,c
[0-9]	یک رقم
** (با globstar)	تمام زیردایرکتوری‌ها
🎯 ساختار کلی اسکریپت Bash

یک اسکریپت کامل معمولاً شامل موارد زیره:

#!/bin/bash
set -euo pipefail

# متغیر
name="ali"

# چاپ
echo "Hello $name"

# شرط
if [ "$name" = "ali" ]; then
  echo "You are Ali"
fi

# حلقه
for f in *; do
  echo "File: $f"
done

🎯 مهم‌ترین دستورات پایه Bash
دستور	کاربرد
echo	چاپ
cd	تغییر مسیر
ls	لیست فایل‌ها
cp	کپی
mv	جابجایی
rm	حذف
mkdir	ساخت پوشه
chmod	تغییر دسترسی
cat	نمایش فایل
grep	جستجو
sed / awk	ویرایش متن
ssh	ورود به سرور
scp	کپی روی سرور





عملگر های مقایسه ای بش
-eq	برابر
-ne	نابرابر
-gt	بزرگ‌تر
-ge	بزرگ‌تر یا مساوی
-lt	کوچک‌تر
-le	کوچک‌تر یا مساوی



[ ]	[ "$a" = "$b" ]	کلاسیک POSIX، امن برای اسکریپت‌های قدیمی
[[ ]]	[[ $a == $b ]]	مدرن، بهتر، پشتیبانی از &&،
(( ))	(( a < b ))	فقط برای اعداد، شبیه C/JS



🚀 بخش 3 — عملگرهای مقایسه رشته‌ای
عملگر	توضیح
=	برابر بودن رشته
==	برابر بودن (در [[ ]] بهتر است)
!=	نابرابر
<	کوچکتر از لحاظ الفبایی
>	بزرگتر از لحاظ الفبایی
-z	خالی بودن رشته
-n	غیرخالی بودن رشته